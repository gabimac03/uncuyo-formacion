# App
PORT=8080
JWT_SECRET=CAMBIA_ESTE_SECRETO_LARGO_Y_UNICO

# DB (ajusta host/puerto según tu server)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=app_user
DB_PASSWORD=app_pass
DB_NAME=portal_alerta

# URL pública (en dev: localhost; en prod: dominio oficial)
APP_BASE_URL=http://localhost:8080

# SMTP (en dev podés dejar vacío y usar Ethereal fallback)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=""
